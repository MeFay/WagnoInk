import { Box } from '@mui/material';

/*const instaPosts = [
  { image: '/images/insta1.jpg', link: 'https://www.instagram.com/p/POST1/' },
  { image: '/images/insta2.jpg', link: 'https://www.instagram.com/p/POST2/' },
  { image: '/images/insta3.jpg', link: 'https://www.instagram.com/p/POST3/' },
];*/

const MoreProjects = () => {
  /*return (
    <Box sx={{ display: 'flex', overflowX: 'auto', gap: 2, px: 2, py: 6 }}>
      {instaPosts.map((post, index) => (
        <Box
          key={index}
          component="a"
          href={post.link}
          target="_blank"
          sx={{
            minWidth: 300,
            height: 300,
            backgroundImage: `url(${post.image})`,
            backgroundSize: 'cover',
            backgroundPosition: 'center',
            borderRadius: 2,
            flexShrink: 0,
            '&:hover': { opacity: 0.8, transition: '0.3s' },
          }}
        />
      ))}
    </Box>
  );*/
};

export default MoreProjects;
